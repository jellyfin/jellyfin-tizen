# Remote Control Keys

If the application maps any remote control keys to custom functions, list the key mappings.

The “Return” and “Exit” key functions are mandatory and common to all applications. Do not modify them.